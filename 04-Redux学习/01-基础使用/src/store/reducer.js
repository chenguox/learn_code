// 1. 初始化数据
const initialState = {
  counter: 0,
};

// 2. 创建一个 reducer 负责修改上面的数据
function reducer(state = initialState, action) {
  return state;
}

module.exports = {
  reducer,
};
// export default reducer;
